<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>MandatoryFieldsJob</Name>
	<SourceCode>
		<Declaration><![CDATA[
class MandatoryFieldsJob
{        
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>main</Name>
				<Source><![CDATA[
    /// <summary>
    /// Runs the class with the specified arguments.
    /// </summary>
    /// <param name = "_args">The specified arguments.</param>
    public static void main(Args _args)
    {  
        SysDictTable    dictTable = new SysDictTable(tableNum(PurchTable));
        SysDictField    dictField;
        TreeNode        treeNode;
        FieldId         fieldId = dictTable.fieldNext(0);

        while (fieldId)
        {
            dictField = dictTable.fieldObject(fieldId);

            if (dictField.isSql() && !dictField.isSystem())
            {
                treeNode = dictField.treeNode();
                /*
            info(strFmt("%1 | %2 | %3",
                dictField.name(),                                               // Field name
                treeNode.AOTgetProperty("Label"),                               // Label id
                SysLabel::labelId2String(treeNode.AOTgetProperty("Label"))));   // Label string
            */
                if (treeNode.AOTgetProperty("Mandatory") == enum2str(NoYes::Yes))
                {
                    info(dictField.name());
                }
            }

            fieldId = dictTable.fieldNext(fieldId);
        }
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>