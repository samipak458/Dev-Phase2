<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>HMClaimAgentViewHMClaimsTableListPage_AssignToMyQueue_Extension</Name>
	<SourceCode>
		<Declaration><![CDATA[
[ExtensionOf(formControlStr(HMClaimAgentViewHMClaimsTableListPage, AssignToMyQueue))]
final class HMClaimAgentViewHMClaimsTableListPage_AssignToMyQueue_Extension
{
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>clicked</Name>
				<Source><![CDATA[
    public void clicked()
    {
        HMBillParameters        hmBillParameters;
        MultiSelectionHelper	multiSelectionHelper;
        FormDataSource          formDatasourcePre,formDatasourcePost;
        int                     position;
        HMClaimTable            claimTable;
        str                     oldValue;
        HMClaimChangeLogTable   hmClaimChangeLogTable;

        select firstonly HMRelatedClaimPrioritization from  hmBillParameters;

        formDatasourcePre = this.formRun().dataSource(FormDatasourceStr(HMClaimAgentViewHMClaimsTableListPage, HMClaimTable));

        if(hmBillParameters.HMRelatedClaimPrioritization == NoYes::Yes)
        {
            multiSelectionHelper = MultiSelectionHelper::construct();
            multiSelectionHelper.parmDatasource(formDatasourcePre);
        }

        claimTable = multiSelectionHelper.getFirst();

        oldValue = claimTable.AgentName;

        next clicked();

        formDatasourcePost = this.formRun().dataSource(FormDatasourceStr(HMClaimAgentViewHMClaimsTableListPage, HMClaimTable));

        multiSelectionHelper = MultiSelectionHelper::construct();
        multiSelectionHelper.parmDatasource(formDatasourcePost);

        claimTable = multiSelectionHelper.getFirst();

        while(claimTable)
        {
            hmClaimChangeLogTable::createClaimChangeLog(claimTable,oldValue);
            
            claimTable = multiSelectionHelper.getNext();
        }
       
        HMClaimTable::AssignSimilarClaims(multiSelectionHelper);

        position = formDatasourcePost.getPosition();

        formDatasourcePost.research();
        formDatasourcePost.refresh();

        formDatasourcePost.setPosition(position);
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>